# Core — install PyTorch with MPS support (macOS arm64 wheels ship MPS by default)
torch>=2.2
torchvision>=0.17

# Config, data, metrics
pyyaml>=6.0
numpy>=1.24
pillow>=10.0
scikit-learn>=1.3      # roc_auc_score, confusion_matrix
pandas>=2.0

# Plots + progress
matplotlib>=3.7
tqdm>=4.65

# Dataset download (FaceForensics++ is via the official script; Kaggle sets via this CLI)
kaggle>=1.6

# FF++ face extraction (scripts/extract_faces_ffpp.py)
# NOTE: install facenet-pytorch with --no-deps — its pins would downgrade torch/torchvision.
facenet-pytorch>=2.6
opencv-python>=4.9,<5   # 5.x removed cv2.CascadeClassifier (Haar fallback)

# Dev
pytest>=7.4
